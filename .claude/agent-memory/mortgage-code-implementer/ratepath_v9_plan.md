- [RatePath v9 Plan Implementation](ratepath_v9_plan.md) — Adds 3 new Pareto objectives (worstCaseInterest / worstCaseAffordabilityBreaches / expectedRefixEventCount), a `worstCaseDefense` composite + 8th weight key, and collapses 10 v6 recommendation cards into 4 (preference + lowestCost + mostStable + worstCaseDefense). Verified: 113/113 vitest pass, eslint clean, tsc only flags pre-existing errors in StrategyDetailModal.js.